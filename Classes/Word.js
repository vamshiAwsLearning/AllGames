class Word{
    //name: Word
    //status: bolean to let user know if this word has been selected already

    constructor(name, color, status){
        this.name = name;
        this.color = color;
        this.status = status;
    }
}

module.exports = Word;